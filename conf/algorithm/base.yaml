# ------------------------------------------------------------------------------
# Base Algorithm Configuration
# ------------------------------------------------------------------------------

defaults:
  - /schedules: base

# Required fields (must be overridden by each algorithm config)
_target_: ???

local_lr: ???
max_epochs_per_round: ???

# Schedules will be populated from defaults list above
# Can still override specific parts:
# schedules:
#   evaluation:
#     post_aggregation:
#       every: 2
